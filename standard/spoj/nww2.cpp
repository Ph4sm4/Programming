#include <bits/stdc++.h>
using namespace std;

int main() {

    int n, k;
    cin >> k;

    for (int i = 0; i < k; i++) {
        cin >> n;
        vector<int> numbers(n);
        for (int j = 0; j < n; j++) {
            int x;
            cin >> x;
            numbers.push_back(x);
        }

        
    }

        return 0;
}